#!/usr/bin/env bash
# Starter plugin PostToolUse hook: demonstrates the hook mechanism only.
# Intentionally has no side effects beyond a stderr log line.
echo "[starter plugin] a Write/Edit tool call just completed" >&2
